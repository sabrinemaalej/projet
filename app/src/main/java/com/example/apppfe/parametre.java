package com.example.apppfe;

import android.annotation.SuppressLint;
import android.os.Bundle;
import android.widget.ArrayAdapter;
import android.widget.ListView;
import android.widget.Toast;

import androidx.appcompat.app.AppCompatActivity;

public class parametre extends AppCompatActivity {

//    private ListView languageListView;
//    private ArrayAdapter<String> languageAdapter;
//
//    @SuppressLint("MissingInflatedId")
//    @Override
//    protected void onCreate(Bundle savedInstanceState) {
//        super.onCreate(savedInstanceState);
//        setContentView(R.layout.sign_up);
//
//       languageListView = findViewById(R.id.languageListViews);
//
//        // Créer un tableau de chaînes représentant les options de langue
//        String[] languages = {"Français", "Anglais", "Espagnol", "Allemand"};
//
//        // Créer un adaptateur pour la liste de langues
//        languageAdapter = new ArrayAdapter<>(this, android.R.layout.simple_list_item_1, languages);
//
//        // Définir l'adaptateur sur le ListView
//        languageListView.setAdapter(languageAdapter);
//
//        // Gérer les clics sur les éléments de la liste
//        languageListView.setOnItemClickListener((parent, view, position, id) -> {
//            String selectedLanguage = (String) parent.getItemAtPosition(position);
//            Toast.makeText(parametre.this, "choisir la lange: " + selectedLanguage, Toast.LENGTH_SHORT).show();
//        });
//    }
}
